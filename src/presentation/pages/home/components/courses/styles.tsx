import styled from 'styled-components'

export const Container = styled.div`
  margin: 30px auto;

  background: #ffffff;
  backdrop-filter: blur(10px);
  border-radius: 10px;
  padding: 5px 32px;

  
  }
`
